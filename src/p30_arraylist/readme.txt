p30_arraylist
1. initial: ArrayList<type> name = new ArrayList<type>();
2. print:string object can be printed directly; so as to Integer but not int, why?
3. add:    index, value, default index=-1
4. remove: index,
5. set:    index, value
6. get:    index,

7. methods: size(),

8.ArrayList 中的元素实际上是对象[引用类型]，如果我们要存储其他类型，只能为引用数据类型，
  这时我们就需要使用到基本类型的包装类, 基本类型对应的包装类表如下[首字母大写]:
  boolean	Boolean
  byte	    Byte
  short	    Short
  int	    Integer
  long	    Long
  float	    Float
  double	Double
  char	    Character

9.